if room == rIntro
{
    str_main+=string_upper(string_char_at(str,string_length(str_main)+1))
    alarm[1] = Clicked
}
if room == rEnd
{
    str_main+=string_upper(string_char_at(str,string_length(str_main)+1))
    alarm[1] = Clicked
}

